meta {
  name: users id -invalid- -Bad Request-
  type: http
  seq: 6
}

get {
  url: {{BASE_URL}}/users/a
  body: none
  auth: none
}

headers {
  X-Authorization: token
}

tests {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
  
}
